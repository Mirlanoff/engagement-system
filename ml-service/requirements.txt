# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.1
pydantic-settings==2.3.0

# Computer Vision
opencv-python-headless==4.9.0.80
numpy==1.26.4
Pillow==10.3.0

# Face analysis
mediapipe==0.10.14
# Real face-recognition embeddings (ArcFace, 512-dim) via InsightFace.
# Runs on CPU through ONNX Runtime — far lighter than the TensorFlow
# stack DeepFace required (~500MB → ~150MB image impact, ~5x faster
# inference). Model weights for the bundled `buffalo_l` pack (detection
# + recognition) are cached to /app/models_cache via INSIGHTFACE_HOME so
# they survive container restarts.
insightface==0.7.3
onnxruntime==1.18.1

# Async задачи
celery==5.4.0
redis==5.0.7
flower==2.0.1

# HTTP клиент
httpx==0.27.0
tenacity==8.3.0

# Метрики
prometheus-client==0.20.0
prometheus-fastapi-instrumentator==7.0.0

# Утилиты
python-multipart==0.0.9
python-dotenv==1.0.1
loguru==0.7.2
structlog==24.2.0
